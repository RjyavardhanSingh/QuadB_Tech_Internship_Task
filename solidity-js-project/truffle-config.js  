module.exports = {
  networks: {
    development: {
      host: "127.0.0.1",
      port: 8545,  // Updated to match Ganache default port
      network_id: "*"
    },
    // Add other networks here
  },
  compilers: {
    solc: {
      version: "0.8.20",  // Updated to a more recent version
      settings: {
        optimizer: {
          enabled: true,
          runs: 200
        }
      }
    }
  },
  // Configure other settings as needed
};